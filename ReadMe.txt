The zip file contains Java script based Sky Tech Test in Cypress
Unzip the file in Cypress workspace
Insall cypress (as npm install), cypress-xpath, cypress-iframe
Three Java script files are available and could be executed all at once or individually using npx cypress open in terminal
